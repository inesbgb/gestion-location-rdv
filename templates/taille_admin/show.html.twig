{% extends 'base.html.twig' %}

{% block title %}Taille{% endblock %}

{% block body %}
    <h1>Taille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ taille.id }}</td>
            </tr>
            <tr>
                <th>Libel</th>
                <td>{{ taille.libel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_taille_admin_index') }}">back to list</a>

    <a href="{{ path('app_taille_admin_edit', {'id': taille.id}) }}">edit</a>

    {{ include('taille_admin/_delete_form.html.twig') }}
{% endblock %}
