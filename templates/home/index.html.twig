{% extends 'base.html.twig' %}

{% block title %}
  Accueil
{% endblock %}

{% block body %}
  <div class="pt-24">
    <div class="container px-3 mx-auto flex flex-wrap flex-col md:flex-row items-center">
      <!-- Colonne de gauche -->
      <div class="flex flex-col w-full md:w-2/5 justify-center items-start text-center md:text-left">
        <p class="uppercase tracking-loose w-full">
          Découvrez notre collection
        </p>
        <h1 class="my-4 text-5xl font-bold leading-tight">
          Louez ou achetez la robe parfaite
        </h1>
        <p class="leading-normal text-2xl mb-8">
          Découvrez notre collection exclusive de robes pour invités et mariées.
          Prenez rendez-vous dès maintenant pour louer la robe parfaite pour
          votre événement ou pour acheter une robe de mariée sur mesure, conçue
          selon vos envies.
        </p>
        <a href="{{ path('app_appointment') }}"
          class="mx-auto lg:mx-0 hover:underline bg-gold text-white text-2xl font-bold rounded-full my-6 py-6 px-10 shadow-lg focus:outline-none focus:shadow-outline transform transition hover:scale-105 duration-300 ease-in-out">
          Prendre Rendez-vous
        </a>
      </div>
      <!-- Colonne de droite -->
       <div class="w-full md:w-3/5 py-6 text-center">
        {% if elementAdmin and elementAdmin.mainImage %}
          <img src="{{ asset('uploads/' ~ elementAdmin.mainImage) }}" alt="Image principale" class="w-full md:w-4/5 mx-auto rounded-lg shadow-lg">
        {% else %}
          <div class="w-full md:w-4/5 h-64 mx-auto bg-gray-200 rounded-lg shadow-lg flex items-center justify-center">
            <span class="text-gray-500">Image non disponible</span>
          </div>
        {% endif %}
      </div>
    </div>
  </div>
  <!-- Section Nouvelle Collection -->
  <div class="pt-24">
  <div class="container px-3 mx-auto">
    <h2 class="text-4xl font-bold text-center mb-8">
      Nouvelle Collection de Robes à Louer
    </h2>
    <div class="relative">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {% for i in 1..3 %}
          {% set carouselImage = attribute(elementAdmin, 'carouselImage' ~ i) %}
          <div class="w-full h-96 bg-gray-800 rounded-lg shadow-lg">
            {% if carouselImage %}
              <img src="{{ asset('uploads/' ~ carouselImage) }}"
                alt="Robe {{ i }}"
                class="w-full h-full object-cover rounded-lg" />
            {% else %}
              <div class="w-full h-full bg-gray-200 flex items-center justify-center rounded-lg">
                <span class="text-gray-500">Image non disponible</span>
              </div>
            {% endif %}
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
</div>
{% endblock %}
