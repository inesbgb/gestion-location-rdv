<form method="post" action="{{ path('app_avis_client_delete', {'id': avis_client.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet avis ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avis_client.id) }}">
    <button class="btn">Supprimer</button>
</form>

