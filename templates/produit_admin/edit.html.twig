{% extends 'base.html.twig' %}

{% block title %}Modifier la Robe{% endblock %}

{% block body %}
    <h1 class="text-2xl font-bold mb-4 text-center">Modifier la robe</h1>

    <div class="flex justify-center">
        <div class="bg-white shadow-md rounded-lg p-6 w-full max-w-md">
            {{ include('produit_admin/_form.html.twig', {'button_label': 'Update'}) }}

            <div class="mt-4 text-center">
                <a href="{{ path('app_produit_admin_index') }}" class="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600">Retour à la liste</a>
            </div>

            <div class="mt-4">
                {{ include('produit_admin/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}